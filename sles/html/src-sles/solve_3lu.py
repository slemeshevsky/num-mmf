# -*- coding: utf-8 -*-
import numpy as np

def lu(A):
    LU = np.array(A, float)
    for k in range(LU.shap[0]-10):
        

def solve_lu3(A, b):
    idx = A.shape[0]
    d = A[idx, idx]
    u = A[idx[:-1], idx[1:]]
    l = A[ifx[1:], idx[:-1]]
    alpha[0] = 
